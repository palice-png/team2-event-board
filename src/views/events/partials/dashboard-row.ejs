<% const ev = row.event; %>
<li id="dashboard-row-<%= ev.id %>" class="p-4 border border-slate-200 rounded-xl bg-slate-50/50">
  <div class="flex flex-col gap-2 sm:flex-row sm:items-start sm:justify-between">
    <div>
      <div class="mb-2">
        <h3 class="text-lg font-semibold mb-1">
          <a
            href="/events/<%= ev.id %>"
            class="text-blue-700 font-semibold no-underline hover:underline transition"
          >
            <%= ev.title %>
          </a>
        </h3>
        <p class="text-sm text-slate-500">Open the full event detail page</p>
      </div>

      <p class="text-sm text-slate-600 mb-1">
        <strong class="text-slate-700">Date:</strong> <%= ev.startDatetime %>
      </p>
      <p class="text-sm text-slate-600 mb-1">
        <strong class="text-slate-700">Category:</strong> <%= ev.category %>
      </p>
      <p class="text-sm text-slate-600 mb-0">
        <strong class="text-slate-700">Attending:</strong>
        <% if (typeof ev.capacity === "number") { %>
          <%= row.attendeeCount %> / <%= ev.capacity %>
        <% } else { %>
          <%= row.attendeeCount %> (no capacity limit)
        <% } %>
      </p>
    </div>
    <div class="flex flex-col gap-2 shrink-0">
      <% if (ev.status === "draft") { %>
        <form
          method="post"
          action="/events/<%= ev.id %>/publish"
          hx-post="/events/<%= ev.id %>/publish"
          hx-target="#dashboard-row-<%= ev.id %>"
          hx-swap="outerHTML"
          class="m-0"
        >
          <input type="hidden" name="viewSource" value="dashboard" />
          <button
            type="submit"
            class="px-3 py-2 rounded-lg bg-blue-600 text-white text-sm font-medium cursor-pointer hover:bg-blue-700"
          >
            Publish
          </button>
        </form>
      <% } %>
      <% if (ev.status === "published") { %>
        <form
          method="post"
          action="/events/<%= ev.id %>/cancel"
          hx-post="/events/<%= ev.id %>/cancel"
          hx-target="#dashboard-row-<%= ev.id %>"
          hx-swap="outerHTML"
          class="m-0"
        >
          <input type="hidden" name="viewSource" value="dashboard" />
          <button
            type="submit"
            class="px-3 py-2 rounded-lg bg-slate-700 text-white text-sm font-medium cursor-pointer hover:bg-slate-800"
          >
            Cancel
          </button>
        </form>
      <% } %>
    </div>
  </div>
</li>
